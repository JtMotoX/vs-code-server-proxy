version: '3'

services:
  nginx:
    image: nginx:1.23
    container_name: vs-code-server-proxy
    restart: always
    ports:
      - '4445:4445'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./cert:/cert:ro
      - ./html:/usr/share/nginx/html
